spring:
  application:
    name: api-sidep-cambios-ipe-a-rut
  cloud:
    config:
      failFast: true
      uri: http://dockerdesa.mineduc.cl/properties-centralizados/
      server:
        native:
          searchLocations: classpath:/